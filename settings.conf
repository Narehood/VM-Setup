# System Setup Menu - Configuration
# AUTO_UPDATE_CHECK: Check for updates on startup (true/false)
AUTO_UPDATE_CHECK="false"

# AUTO_APPLY_UPDATES: When update checks are enabled, apply fast-forward updates
# without an extra confirmation prompt (true/false)
AUTO_APPLY_UPDATES="false"
